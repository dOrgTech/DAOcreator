{
  "name": "root",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "start": "lerna exec --ignore @dorgtech/daocreator-lib -- yarn start",
    "build": "lerna exec -- yarn build"
  },
  "devDependencies": {
    "lerna": "^3.18.4"
  },
  "workspaces": {
    "packages": [
      "./packages/*"
    ],
    "nohoist": [
      "./packages/lib"
    ]
  }
}
